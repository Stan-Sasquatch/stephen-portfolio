import React, { useState } from 'react';
import Grid from '../../Components/Grid';
import { createNewMinesweeperGrid, createMinesweeperGameRow } from '../../Utils/Functions';

const Minesweeper = () => {
    //     const [mines, setMines] = useState(4);
    //     const [rows, setRows] = useState(5);
    //     const [columns, setColumns] = useState(5)

    //     const onClick = (tile) => {
    //         alert(tile == 1 ? "mine" : "safe")
    //     }


    //     return (<div><Grid array={createNewMinesweeperGrid(mines, rows, columns)} onClick={onClick} createRowFunc={(a, b, c) => { createMinesweeperGameRow(a, b, c) }} /></div>)

    // }
    return (<div>hello world</div>)
}
export default Minesweeper